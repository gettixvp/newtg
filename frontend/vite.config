export default defineConfig({
  plugins: [react()],
  server: { port: 3000 },
  build: { 
    outDir: '../backend/static', // Собранные файлы попадут в backend
    emptyOutDir: true 
  },
  publicDir: 'public' // ← Указывает на папку public
});